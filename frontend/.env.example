VITE_API_URL=http://localhost:8000
VITE_APP_NAME=Newsroom MCP

# Google OAuth — must match backend GOOGLE_CLIENT_ID
# Loads the GIS script in index.html and enables the "Continue with Google" button
VITE_GOOGLE_CLIENT_ID=
